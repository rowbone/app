
1、属性
	Property			Class										Descripition

	$valid				ng-valid 								{Boolean}当验证规则通过时为True
	$invalid			ng-invalid							{Boolean}当验证规则不通过时为True
	$pristine			ng-pristine							{Boolean}当输入元素没被填写过
	$dirty				ng-dirty								{Boolean}当输入元素被填写过

2、组件
	ui-bootstrap：
	ui-router
	bindonce(2000个绑定,ng-repeat的性能问题)
	angular-sanitize:$sce(Strict Contextual Escaping) service支持，过滤html
	angular-animate:动画支持
	angular-growl：简单的消息提醒
	angular-base64：angular的base64实现
	angular-translate：angular的多语言支持
	angular-cookies：操作cookies
	html2js：将html打包成js文件。
