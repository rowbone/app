
2015-04-03:
1、增加filter
	截取字符串、数字

2、增加校验（指令）
	身份证等证件、特定数字校验

3、directive的更详细用法（整理文档）

4、AngularJS中遇到的问题（整理文档）

2015-04-07:
1、查看源码angular-ui中$modal服务的依赖（resolve）实现参数传递的方式

2、ng-show、ng-hide、ng-if 对验证的影响

3、组件形成的页面元素（日期选择、地区选择）的验证

2015-04-10:
1、表单验证的问题
	提交按钮触发时，自动focus到第一个不通过的元素；
	ng-include 指令引进的页面校验不到；
	ng-repeat 指令生成的元素校验不到。

2015-04-13：
1、封装弹出层样式，统一PC端和移动端

2、移动端弹出层，只需要内容滚动，层不滚动